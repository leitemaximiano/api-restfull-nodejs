const assert = require('assert');

describe('Testando a aplicação', () => {
    it('Primeiro teste', async() => {
        const espected = 0;
        const result = 0;

        assert.deepEqual(result, espected);
    });
});